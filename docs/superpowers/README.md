# Superpowers Specs

This directory contains local design/specification documents produced during AI-assisted development.

Tracked:

- `specs/` — durable design specs that can be reviewed and referenced.

Ignored:

- `plans/` — execution plans, scratch task lists, and local workflow files.
