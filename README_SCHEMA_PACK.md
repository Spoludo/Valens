# Valens Schema + Isometric Foundations Pack

Extract this archive at the repository root.

It adds:

```text
schemas/
exercise-packs/bundled/isometric-foundations/
scripts/validate-exercise-pack.py
```

After extracting, run:

```bash
python3 scripts/validate-exercise-pack.py
```

Suggested commit:

```bash
git add schemas exercise-packs/bundled scripts/validate-exercise-pack.py README_SCHEMA_PACK.md
git commit -m "spec(schema): add exercise schema and isometric foundations pack"
```
